require('dotenv').config();
const http = require("http");
const app = require("../src/app");

http.createServer(app.callback()).listen(process.env.PORT || 3000, function(){
  console.log("server running on port", process.env.PORT || 3000);
});